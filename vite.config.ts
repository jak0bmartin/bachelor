import { defineConfig } from 'vite';

export default defineConfig({
  root: '.',
  base: '/chemie-game/', // Ersetze 'chemie-game' mit deinem Repository-Namen
  build: {
    outDir: 'dist',
  },
});




